function About(){
    return <div>Cuando creamos una carpeta dentro de la carpeta app lo que hacemos es generar una ruta, 
        dentro de cada una deberemos crear un archivo page.jsx el cual contendra la pagina a mostrar por defecto.</div>
}

export default About;